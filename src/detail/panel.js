// Person detail + sources panel — placeholder for step 2.

export function renderPersonDetail(/* container, person, model */) {
  throw new Error('renderPersonDetail() not implemented yet — step 2.');
}
