// Tree view — placeholder for step 2.
// Will render an interactive family tree from the normalized model.

export function renderTree(/* container, model */) {
  throw new Error('renderTree() not implemented yet — step 2.');
}
